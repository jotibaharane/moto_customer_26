import { fontScale } from './responsive';

export const FontSize = {
  xs: fontScale(10),
  sm: fontScale(12),
  md: fontScale(14),
  base: fontScale(16),
  lg: fontScale(18),
  xl: fontScale(20),
  xxl: fontScale(24),
  title: fontScale(28),
  hero: fontScale(32),
};
